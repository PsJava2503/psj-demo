server:
  port: 8084

spring:
  application:
    name: psj-commerce-product-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

dubbo:
  application:
    name: ${spring.application.name}
  protocol:
    name: dubbo
    port: 20882
  registry:
    address: nacos://localhost:8848
  scan:
    base-packages: com.psj.commerce.product
