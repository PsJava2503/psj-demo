server:
  port: 8087

spring:
  application:
    name: psj-commerce-address-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

dubbo:
  application:
    name: ${spring.application.name}
  protocol:
    name: dubbo
    port: 20885
  registry:
    address: nacos://localhost:8848
  scan:
    base-packages: com.psj.commerce.address
